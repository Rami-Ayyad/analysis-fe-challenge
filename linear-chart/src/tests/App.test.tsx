import React from 'react';
import { render, screen } from '@testing-library/react';
import App from '../components/App/App';

// describe("App components elements", () => {

//   it("renders the main title of the app", () => {
//     render(<App/>)
//     const mainTitleElement = screen.getByText(/Analysis Chart/i)
//     expect(mainTitleElement).toBeInTheDocument();
//   })

//   it("renders the second title of the app", () => {
//     render(<App/>)
//     const secondTitleElement = screen.getByText(/Number of Lessons/i)
//     expect(secondTitleElement).toBeInTheDocument();
//   })

// })
